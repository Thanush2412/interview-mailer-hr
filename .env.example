# Only 2 env vars needed. Everything else (GAS URL, Sheet URL, Google Client Secret,
# Session Secret, admin emails, column mapping) is stored in and managed via the DB.

DATABASE_URL=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
